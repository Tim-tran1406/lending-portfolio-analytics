# Install into a Python 3.12 virtual environment (dbt does not yet support 3.14):
#
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Pin dbt-core to the stable line (its dep chain otherwise pulls a beta).
dbt-core>=1.9,<1.12
dbt-postgres>=1.8
pandas>=2.2
psycopg2-binary>=2.9
SQLAlchemy>=2.0
python-dotenv>=1.0
tqdm>=4.66
