version: 2

sources:
  - name: raw
    description: "Raw LendingClub data loaded from the CSV by the Python ingest script."
    schema: raw
    tables:
      - name: loans
        description: "One row per accepted loan; columns kept as-is from the source file."
